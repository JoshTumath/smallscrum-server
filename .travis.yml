---
language: node_js
node_js:
  - "4"

sudo: false

cache:
  directories:
    - .ember
    - node_modules

before_install:
  - npm config set spin false

install:
  - npm install -g bower
  - npm install
  - npm run-script build

script:
  - npm test
